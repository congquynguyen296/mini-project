server:
  port: 8080

spring:
  datasource:
    url: ${DBMS_URL:jdbc:mysql://localhost:3306/mini_project}
    username: ${DBMS_USERNAME:root}
    password: ${DBMS_PASSWORD:root}
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true